---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className = "", ...props } = Astro.props;

const classes = cn("leading-none font-semibold", className);
---

<div data-slot="card-title" class={classes} {...props}>
  <slot />
</div>
