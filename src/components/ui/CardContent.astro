---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className = "", ...props } = Astro.props;

const classes = cn("px-6", className);
---

<div data-slot="card-content" class={classes} {...props}>
  <slot />
</div>
