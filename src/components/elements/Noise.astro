
---
import { cn } from '@/lib/utils';

const {className} = Astro.props;
---

<div
  class={cn(
    'pointer-events-none absolute inset-0 bg-[url(/images/noise.svg)] bg-cover bg-center bg-no-repeat opacity-20',
    className,
  )}
/>
